# Same-origin API prefix. This is public build-time configuration, not a secret.
VITE_API_BASE_URL=/api

# Vite forwards local /api requests to the production backend.
API_PROXY_TARGET=https://api.crl-transport.com
